/// App-wide constants.
class AppConstants {
  AppConstants._();

  /// App name.
  static const appName = 'Finsight';

  /// App version.
  static const appVersion = '0.1.0';

  /// Database name.
  static const dbName = 'finsight_db';

  /// Default number of recent transactions to show on dashboard.
  static const dashboardRecentTransactionCount = 10;

  /// Date format for display.
  static const displayDateFormat = 'dd MMM yyyy';

  /// Date format for storage (ISO).
  static const storageDateFormat = 'yyyy-MM-dd';
}
